"""
Student 4 — Reproduction script: Triggers downstream code crash via bad state data

TODO: write a deterministic script that feeds malformed/unvalidated data
from Worker B directly into downstream logic WITHOUT the sanitization node,
showing a runtime crash (e.g. a KeyError or TypeError).
"""

if __name__ == "__main__":
    raise NotImplementedError("TODO: reproduce the downstream cascade failure here.")
